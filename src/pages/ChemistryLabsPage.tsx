import { LabList } from "@/components/labs/LabList";

export default function ChemistryLabsPage() {
  return <LabList subject="chemistry" title="Chemistry Labs" subtitle="Pour, mix, observe — real reactions, virtually." />;
}
